#!/usr/bin/env bash

if [ "$1" == "sync" ]; then
    emacsclient -e "(kill-emacs)"
    "$HOME"/.config/emacs/bin/doom "$@"
    emacsclient -c -n --alternate-editor=""
else
    "$HOME"/.config/emacs/bin/doom "$@"
fi
