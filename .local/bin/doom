#!/usr/bin/env bash

read -r default_profile <"$HOME/.config/chemacs/profile"

if [[ "$1" == "sync" && "$#" == 1 ]]; then
    emacsclient -s "$default_profile" -e "(kill-emacs)"
    /opt/doom/bin/doom "$@"
    emacsclient -c -s "$default_profile" -n -a=''
else
    /opt/doom/bin/doom "$@"
fi
